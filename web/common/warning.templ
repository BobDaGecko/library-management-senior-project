package common

// WarningBanner is a generic, reusable warning alert component.
// Use it from any handler with fail.RenderPartial or inside other components.
templ WarningBanner(message string) {
	<div class="alert alert-warning mb-4">
		<strong>Warning:</strong> { message }
	</div>
}
